# ==============================================================================
# UdaanSetu.AI - Environment Variables Example Template
# ==============================================================================
# Instructions:
# 1. Copy this file to '.env' in the appropriate directory (frontend or backend).
# 2. Replace the placeholder values with your actual credentials.
# 3. DO NOT COMMIT YOUR ACTUAL .ENV FILE!
# ==============================================================================

# ------------------------------------------------------------------------------
# BACKEND CONFIGURATION (.env in /backend folder)
# ------------------------------------------------------------------------------

# Google AI Studio API Key (Get from: https://aistudio.google.com/)
GEMINI_API_KEY=your_gemini_api_key_here

# Assessment Logic Configuration
# Controls how many questions are asked during the adaptive test
ASSESSMENT_QUESTION_COUNT=15

# Firebase Configuration
# Paste the content of your serviceAccountKey.json as a minified string here
# This is used as a fallback if the physical .json file is not present.
FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", "project_id": "...", ...}'

# Database Configuration (Optional - for RAG/PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# External Platform URLs
PUBLIC_URL=https://your-app-url.vercel.app


# ------------------------------------------------------------------------------
# FRONTEND CONFIGURATION (.env in /frontend folder)
# ------------------------------------------------------------------------------
# All frontend variables must start with NEXT_PUBLIC_ to be accessible in the browser.

# Firebase Web Config (Get from: Firebase Console > Project Settings)
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
